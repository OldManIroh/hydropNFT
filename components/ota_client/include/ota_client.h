/**
 * @file ota_client.h
 * @brief Заголовочный файл компонента OTA (Over-The-Air) обновлений
 * 
 * Этот компонент реализует безопасное обновление прошивки "по воздуху" для ESP32.
 * 
 * Основные возможности:
 * - Загрузка новой прошивки по HTTPS
 * - Запись во второй OTA раздел flash-памяти
 * - Проверка целостности загруженной прошивки
 * - Автоматический откат к предыдущей версии при ошибке
 * - Диагностика работоспособности после обновления
 * 
 * Архитектура разделов flash-памяти ESP32:
 * @verbatim
 * +------------------+
 * | Bootloader       | 0x1000
 * +------------------+
 * | Partition Table  | 0x8000
 * +------------------+
 * | OTA_0 (app)      | 0x10000  <- Текущая прошивка
 * +------------------+
 * | OTA_1 (app)      |          <- Резервная/обновляемая
 * +------------------+
 * | NVS (data)       |          <- Настройки, Wi-Fi креды
 * +------------------+
 * @endverbatim
 * 
 * Процесс OTA обновления:
 * 1. Загрузка бинарника прошивки с HTTPS сервера
 * 2. Проверка заголовка (версия, совместимость)
 * 3. Запись в OTA_1 раздел
 * 4. Валидация контрольной суммы
 * 5. Установка OTA_1 как загрузочного
 * 6. Перезагрузка
 * 7. Диагностика (5 сек на проверку GPIO)
 * 8. Подтверждение или откат
 * 
 * Пример использования в main.c:
 * @code
 * void app_main(void) {
 *     // 1. Проверка статуса прошивки после перезагрузки
 *     ota_check_and_diagnose();
 *     
 *     // 2. Инициализация сети и NVS
 *     ota_init();
 *     
 *     // 3. Запуск OTA при получении команды из MQTT
 *     if (mqtt_get_ota_flag()) {
 *         ota_start_task();
 *     }
 * }
 * @endcode
 * 
 * @author HydroNFT Team
 * @version 2.0
 * @date 2026
 */

#pragma once

#include <stdint.h>
#include <stdbool.h>
#include <esp_err.h>
#include <freertos/task.h>

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Проверка состояния OTA и запуск диагностики при необходимости
 * 
 * Вызывается ПЕРВОЙ в app_main() сразу после старта.
 * Проверяет статус текущей прошивки в NVS.
 * 
 * Если прошивка имеет статус ESP_OTA_IMG_PENDING_VERIFY:
 * - Это значит, что устройство перезагрузилось после OTA обновления
 * - Запускается диагностика (проверка GPIO в течение 5 секунд)
 * - При успехе: прошивка подтверждается (ESP_OTA_IMG_VALID)
 * - При неудаче: откат к предыдущей версии и перезагрузка
 * 
 * Если статус ESP_OTA_IMG_VALID или ESP_OTA_IMG_INVALID:
 * - Прошивка уже проверена ранее
 * - Диагностика не требуется
 * 
 * Также выводит SHA-256 хеши для отладки:
 * - Таблицы разделов
 * - Загрузчика
 * - Текущей прошивки
 * 
 * @note Вызывается один раз в начале app_main()
 * @note Диагностика использует GPIO из CONFIG_EXAMPLE_GPIO_DIAGNOSTIC
 * 
 * @see ota_diagnostic()
 * @see esp_ota_get_state_partition()
 */
void ota_check_and_diagnose(void);

/**
 * @brief Инициализация OTA компонента (NVS, сеть)
 * 
 * Инициализирует необходимые подсистемы для работы OTA:
 * 
 * 1. NVS (Non-Volatile Storage):
 *    - Хранит состояние OTA (какой раздел загрузочный)
 *    - Хранит Wi-Fi креды (SSID, пароль)
 *    - Хранит пользовательские настройки
 *    - При несовпадении формата - очищает и пересоздаёт
 * 
 * 2. Сетевой стек:
 *    - esp_netif - абстрактный сетевой интерфейс
 *    - esp_event_loop - цикл обработки сетевых событий
 * 
 * 3. Подключение к сети:
 *    - Wi-Fi или Ethernet (в зависимости от конфигурации)
 *    - Блокирует выполнение до получения IP-адреса
 * 
 * 4. Настройка Wi-Fi:
 *    - Отключает энергосбережение (WIFI_PS_NONE)
 *    - Максимальная производительность для OTA
 * 
 * @note Вызывается один раз после ota_check_and_diagnose()
 * @note Требуется перед запуском ota_start_task()
 * 
 * @see ota_start_task()
 */
void ota_init(void);

/**
 * @brief Запуск задачи OTA-обновления
 *
 * Создаёт новую задачу FreeRTOS для выполнения OTA обновления.
 *
 * Процесс обновления в задаче:
 * 1. Подключение к HTTPS серверу (из CONFIG_EXAMPLE_FIRMWARE_UPG_URL)
 * 2. Загрузка бинарника прошивки порциями по 1024 байта
 * 3. Проверка заголовка прошивки (версия, совместимость)
 * 4. Проверка: не та же ли версия уже установлена?
 * 5. Проверка: не является ли версия "невалидной"?
 * 6. Запись данных во flash (OTA раздел)
 * 7. Валидация контрольной суммы образа
 * 8. Установка нового раздела как загрузочного
 * 9. Перезагрузка устройства
 *
 * @return ESP_OK при успешном создании задачи OTA
 * @return ESP_ERR_NO_MEM если не удалось создать задачу
 * @return ESP_ERR_INVALID_STATE если OTA уже выполняется
 *
 * @note Функция не блокирующая - возвращается сразу после создания задачи
 * @note Задача имеет приоритет 5 (выше среднего)
 * @note Размер стека задачи: 8192 байт
 * @note После успешной загрузки устройство ПЕРЕЗАГРУЗИТСЯ
 *
 * @note Конфигурация (через menuconfig):
 * - CONFIG_EXAMPLE_FIRMWARE_UPG_URL - URL прошивки
 * - CONFIG_EXAMPLE_OTA_RECV_TIMEOUT - таймаут получения (мс)
 * - CONFIG_EXAMPLE_GPIO_DIAGNOSTIC - GPIO для диагностики
 * - CONFIG_EXAMPLE_CONNECT_WIFI - использовать Wi-Fi
 *
 * @see ota_init()
 */
esp_err_t ota_start_task(void);

/**
 * @brief Запуск задачи публикации прогресса OTA в MQTT
 *
 * Создаёт динамическую задачу, которая публикует прогресс OTA
 * в MQTT. Задача завершает себя после окончания OTA сессии.
 *
 * @note Вызывается после ota_start_task()
 */
void start_ota_progress_task(void);

/**
 * @brief Функция диагностики работоспособности после обновления
 * 
 * Вызывается после первого запуска новой прошивки.
 * Даёт 5 секунд на проверку аппаратной части через GPIO пин.
 * 
 * Процесс диагностики:
 * 1. Настройка диагностического GPIO на вход с подтяжкой к VCC
 * 2. Ожидание 5 секунд (vTaskDelay)
 * 3. Чтение уровня сигнала
 * 4. Сброс настроек GPIO
 * 
 * Результаты:
 * - GPIO = HIGH (1): диагностика успешна
 * - GPIO = LOW (0): проблема, требуется откат
 * 
 * Практическое применение:
 * - Подключить кнопку для ручного подтверждения
 * - Подключить датчик для проверки работы устройства
 * - Подключить сигнал от другой платы о состоянии системы
 * 
 * @return true если диагностика успешна (GPIO HIGH)
 * @return false если обнаружена проблема (GPIO LOW)
 * 
 * @note Использует CONFIG_EXAMPLE_GPIO_DIAGNOSTIC для номера GPIO
 * @note Вызывается из ota_check_and_diagnose()
 * 
 * @see ota_check_and_diagnose()
 * @see esp_ota_mark_app_valid_cancel_rollback()
 * @see esp_ota_mark_app_invalid_rollback_and_reboot()
 */
bool ota_diagnostic(void);

/**
 * @brief Получить текущий прогресс OTA обновления в процентах
 * @return Прогресс от 0 до 100, или -1 если OTA не активна
 */
int ota_get_progress_percent(void);

/**
 * @brief Получить общий размер загружаемого файла в байтах
 * @return Размер файла в байтах, или 0 если неизвестно
 */
int ota_get_total_size(void);

/**
 * @brief Получить количество загруженных байт
 * @return Количество загруженных байт
 */
int ota_get_downloaded_bytes(void);

/**
 * @brief Проверить, активна ли OTA сессия
 * @return true если OTA обновление запущено (даже если загрузка ещё не началась)
 */
bool ota_is_active(void);

#ifdef __cplusplus
}
#endif
